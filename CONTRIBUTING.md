# Contributing Guidelines

感谢你参与这个项目。为了让协作成本尽量低，请尽量遵循下面的约定：

## 开发原则

- 优先做最小改动，避免不必要的重构。
- 保持现有代码风格和目录结构，不要在无关文件里顺手改格式。
- 新增功能时，优先补充文档或示例，再考虑代码抽象。
- 不要提交密钥、Token、私有地址或本地路径配置。

## 提交流程

1. 先在本地验证改动是否符合预期。
2. 如果是行为变更，尽量附上输入、输出或截图说明。
3. 如果是文档变更，说明对应的使用场景和影响范围。
4. 一个 PR 尽量只解决一类问题，避免混入无关修改。

## 代码规范

- Python 代码保持清晰、直接，避免过度抽象。
- 尽量沿用现有命名和模块边界。
- 对路由、工具执行、消息总线等核心逻辑的改动，要说明回归风险。
- 新增配置项时，必须同步更新 README 或技术报告。

## 社区维护计划

- 以 issue 驱动需求收集和缺陷跟踪。
- 以 PR 方式进行代码评审和合并。
- 对重要变更保留变更说明，便于回溯。
- 后续如引入稳定版本发布，可增加 changelog、release note 和 code of conduct。
